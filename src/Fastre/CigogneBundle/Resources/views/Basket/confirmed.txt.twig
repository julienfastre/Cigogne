{{ 'cigogne.basket.confirmed.thanks'|trans }}
		
{{ 'cigogne.basket.confirmed.find_html'|trans({ '%url%': confirmationUrl }) }}
		
{%  if totalMoney > 0 %}
{{ 'cigogne.basket.confirmed.make_transfer_IBAN'|trans({ '%amount%' : totalMoney, '%IBAN%': list.creator.paymentBankAccountNumber})   }}
{%  endif %}


{% if giftMoneys | length > 0 %}
Vous donnez de l'argent pour...
-------------------------------

{% for giftMoney in giftMoneys %}- {{giftMoney.amount| number_format(2, ',', ' ')}} € : {{giftMoney.item.title}}
{% endfor %}


{% endif %}

{% if giftNatures | length > 0 %}
Vous apporterez les objets suivants...
--------------------------------------

{% for giftNature in giftNatures %}- {{giftNature.quantity}} :  {{giftNature.item.title}}{% if giftNature.message is not empty %} 
   Votre message : 
   {{ giftNature.message }} 
{% endif %}
{% endfor %}

{% endif %}

{% if giftServices | length > 0 %}    
Vous rendrez ces services
--------------------------

{% for giftService in giftServices %}- {{giftService.quantity}}: {{giftService.item.title}}, {% if giftService.message is not empty %}
   Votre message :
   {{ giftService.message }}
{% endif %}
{% endfor %}

{% endif %}

Soit... 
{{totalMoney | number_format(2, ',', ' ')}}€ pour {{giftMoneys | length }} objet(s),
   {{giftNatures | length }} dons d'objets et {{giftServices | length }} services.